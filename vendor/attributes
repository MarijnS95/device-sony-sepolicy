# All types in /mnt/vendor/persist
attribute vendor_persist_type;

attribute per_mgr_client;
# The client attribute is only used in the vendor sepolicy and can be optimized away by the policy compiler:
expandattribute per_mgr_client true;
